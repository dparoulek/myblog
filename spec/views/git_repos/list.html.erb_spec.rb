require 'spec_helper'

describe "/git_repos/list.html.erb" do
  include GitReposHelper
 
#   before(:each) do
#     assigns[:cwd] = 
#   end

#   it "renders the list of dirs and files inside git_repo" do
#     response.should have_tag("form[action=#{git_repo_path(@git_repo)}][method=post]") do
#       with_tag('input#git_repo_path[name=?]', "git_repo[path]")
#     end
#   end
end
